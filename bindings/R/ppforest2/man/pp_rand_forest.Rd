% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{pp_rand_forest}
\alias{pp_rand_forest}
\title{Parsnip model specification for pprf.}
\usage{
pp_rand_forest(
  mode = "classification",
  trees = NULL,
  mtry = NULL,
  penalty = NULL
)
}
\arguments{
\item{mode}{A character string for the model type. Only \code{"classification"} is supported.}

\item{trees}{The number of trees in the forest (maps to \code{size}).}

\item{mtry}{The number of variables to consider at each split (maps to \code{n_vars}).}

\item{penalty}{The regularization parameter (maps to \code{lambda}).}
}
\value{
A parsnip model specification.
}
\description{
Creates a model specification for a Projection Pursuit random forest.
Use \code{set_engine("ppforest2")} to select the ppforest2 engine.
}
\examples{
\dontrun{
library(parsnip)
spec <- pp_rand_forest(trees = 50, mtry = 2) \%>\% set_engine("ppforest2")
fit <- spec \%>\% fit(Type ~ ., data = iris)
predict(fit, iris)
predict(fit, iris, type = "prob")
}
}
\seealso{
\code{\link{pprf}} for the underlying training function, \code{\link{pp_tree}} for single trees
}
