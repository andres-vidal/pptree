% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json.R
\name{save_json}
\alias{save_json}
\alias{save_json.pptr}
\alias{save_json.pprf}
\title{Save a model to a JSON file.}
\usage{
save_json(model, path, ...)

\method{save_json}{pptr}(model, path, include_metrics = TRUE, ...)

\method{save_json}{pprf}(model, path, include_metrics = TRUE, ...)
}
\arguments{
\item{model}{A \code{pptr} or \code{pprf} model.}

\item{path}{File path to write the JSON to.}

\item{...}{Additional arguments (currently unused).}

\item{include_metrics}{If \code{TRUE} (default), include variable importance
and (for forests) OOB error in the output. Set to \code{FALSE} to save
only the model structure and metadata.}
}
\description{
Serializes a \code{pptr} or \code{pprf} model to JSON format compatible
with the C++ CLI. The JSON includes the model structure, group labels,
training parameters, and optionally variable importance metrics.
}
\examples{
model <- pptr(Type ~ ., data = iris, seed = 42)
path <- tempfile(fileext = ".json")
save_json(model, path)

}
\seealso{
\code{\link{load_json}}, \code{\link{pptr}}, \code{\link{pprf}}
}
